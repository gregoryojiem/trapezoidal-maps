from dir_acyc_graph import Node, Internal, Leaf
from trapezoidal_map import Trapezoid


def handle_case1(p, seg, leaf):
    pass
    # lr = Node(Leaf(Trapezoid))
    # if is_left_end_point:
    #     pl = lr
    #     pr = s
    # else:
    #     pl = s
    #     pr = lr
    # p = Internal(pl, pr)
